    Real Project (Pandas দিয়ে Mini ML Workflow)

     simple project  একটা  👇
            👉 Student Result Analysis

                    📌 Step 1: Data Load
                            import pandas as pd

                            df = pd.read_csv("students.csv")
                            print(df.head())

                            👉 ধরো data এমন:

                            Name,Marks,City
                            A,80,Dhaka
                            B,45,Khulna
                            C,90,Dhaka
                            D,60,Rajshahi

                    📌 Step 2: Data Clean
                            # null check
                            print(df.isnull().sum())

                            # fill null
                            df["Marks"] = df["Marks"].fillna(df["Marks"].mean())

                    📌 Step 3: Feature Engineering ⭐

                            👉 নতুন column তৈরি

                            df["Result"] = df["Marks"].apply(lambda x: "Pass" if x >= 50 else "Fail")

                    📌 Step 4: Data Analysis
                            👉 Average Marks
                            print(df["Marks"].mean())
                            👉 City wise average
                            print(df.groupby("City")["Marks"].mean())
                            👉 Pass/Fail count
                            print(df["Result"].value_counts())

                    📌 Step 5: Sorting
                            df = df.sort_values(by="Marks", ascending=False)
                            
                    📌 Step 6: Final Data Save
                            df.to_csv("final_result.csv", index=False)

                    🎯 Full Code (Mini Project)

                            import pandas as pd

                            df = pd.read_csv("students.csv")

                            # clean
                            df["Marks"] = df["Marks"].fillna(df["Marks"].mean())

                            # feature
                            df["Result"] = df["Marks"].apply(lambda x: "Pass" if x >= 50 else "Fail")

                            # analysis
                            print("Average:", df["Marks"].mean())
                            print(df.groupby("City")["Marks"].mean())
                            print(df["Result"].value_counts())

                            # sort
                            df = df.sort_values(by="Marks", ascending=False)

                            # save
                            df.to_csv("final_result.csv", index=False)


                    🔥 Real ML Workflow (Important বুঝো)

                    👉 Pandas দিয়ে আসলে এই flow follow করা হয়:

                            Data Load
                            Data Clean
                            Feature Engineering
                            Data Analysis
                            Model (ML)
                            Save Result

    Next part:
                2.NumPy (math operations)
                3.Matplotlib / Seaborn (graph)
                4.Scikit-learn (ML model)